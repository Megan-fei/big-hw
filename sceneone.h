#ifndef SCENEONE_H
#define SCENEONE_H

#include <QMainWindow>

class Sceneone : public QMainWindow
{
    Q_OBJECT
public:
    explicit Sceneone(QWidget *parent = nullptr);

signals:

public slots:
};

#endif // SCENEONE_H